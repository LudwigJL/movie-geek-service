server:
  port: 4000
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: never
    include-exception: false

spring:
  datasource:
    url: jdbc:postgresql://ep-wandering-glitter-a27a028l.eu-central-1.aws.neon.tech:5432/movie-database
    username: experisdb_owner
    password: KAHCMdG0nfT4
    max-active: 3
    max-idle: 3

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

